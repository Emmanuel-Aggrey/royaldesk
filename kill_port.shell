kill -9 $(sudo lsof -t -i:8001)

sudo kill -9 `sudo lsof -t -i:8002`

#GET PID ON PORT
lsof -i tcp:port

# KILL THE PORT
kill -9 port


START CELERY
celery -A  HRMSPROJECT celery -l INFO

celery -A  HRMSPROJECT beat -l INFO


celery flower --port=5566
celery -A proj flower  --address=127.0.0.6 --port=5566